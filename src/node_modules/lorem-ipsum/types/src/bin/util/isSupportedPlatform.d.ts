/**
 * @param platform  The process platform.
 * @returns         True if the process platform is supported.
 */
declare const isSupportedPlatform: (platform: string) => boolean;
export default isSupportedPlatform;
